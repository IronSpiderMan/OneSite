"""Model metadata parsing shared by introspection and generation phases.

These modules validate and normalize declarations without generating files.
The model introspection entry point remains in ``onesite.codegen.introspect``.
"""
