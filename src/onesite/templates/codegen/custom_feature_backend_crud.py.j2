"""Developer-owned data access for the {{ feature_name }} custom feature."""


async def list_{{ feature_name }}_items() -> list[dict]:
    """Return a safe empty result until project-specific persistence is added."""
    return []
