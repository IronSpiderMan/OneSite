"""Database event listeners package.

Auto-generated — imports all model event listener modules so that
SQLAlchemy ``@event.listens_for`` decorators are registered on import.
"""
{% for model in event_models %}
from . import {{ model.module_name }}
{% endfor %}
