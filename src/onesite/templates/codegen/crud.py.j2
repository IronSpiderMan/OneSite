from typing import Optional, List, Dict, Any
from datetime import datetime, timezone{% if model.fields | selectattr('ui_type', 'equalto', 'time') | list %}, time{% endif %}
from sqlmodel import select, func, col
from sqlmodel.ext.asyncio.session import AsyncSession
from app.core.datetime import to_utc
from app.core.db import transactional_write
from app.core.model_hooks import ModelHookContext, invoke_model_hook, snapshot_model
from app.models.{{ model.source_module }} import {{ model.name }}{% if model.property_config_meta %}, {{ model.property_config_meta.config_class }}{% endif %}
{% if model.timescaledb_entity_model and model.timescaledb_entity_target_table %}
from app.models.{{ model.timescaledb_entity_target_table }} import {{ model.timescaledb_entity_model }}
{% endif %}
{% if model.property_config_meta %}
from app.models.{{ model.property_config_meta.blueprint_module }} import {{ model.property_config_meta.blueprint_model }}
{% endif %}
from app.schemas.{{ model.module_name }} import {{ model.name }}Create{% if not model.is_timescaledb %}, {{ model.name }}Update{% endif %}
{% if model.m2m_fields %}
{% for m2m in model.m2m_fields %}
from app.models.{{ m2m.link_module }} import {{ m2m.link_model }}
from app.models.{{ m2m.target_source_module }} import {{ m2m.target_model }}
{% endfor %}
{% endif %}
{% if model.reverse_m2m %}
{% for rel in model.reverse_m2m %}
from app.models.{{ rel.source_source_module }} import {{ rel.source_model }}
{% endfor %}
{% endif %}
{% for rfk in model.reverse_foreign_keys %}
{% if rfk.editor == 'inline' %}
from app.models.{{ rfk.source_module }} import {{ rfk.source_model }}
{% endif %}
{% endfor %}

{% set scalar_json_arrays = model.fields | selectattr('json_item_kind') | list %}
{% set datetime_fields = model.fields | selectattr('ui_type', 'equalto', 'datetime') | list %}
{% if datetime_fields %}
def _datetime_column_uses_timezone(field_name: str) -> bool:
    column = {{ model.name }}.__table__.columns.get(field_name)
    return bool(column is not None and getattr(column.type, "timezone", False))


def _normalize_datetime_for_storage(field_name: str, value: datetime) -> datetime:
    """Store UTC using the awareness expected by the mapped SQL column."""
    normalized = to_utc(value)
    if not _datetime_column_uses_timezone(field_name):
        return normalized.replace(tzinfo=None)
    return normalized


def _normalize_datetimes(data: dict) -> dict:
    """Interpret naive form values in the system timezone and persist UTC."""
    {% for field in datetime_fields %}
    if isinstance(data.get("{{ field.name }}"), datetime):
        data["{{ field.name }}"] = _normalize_datetime_for_storage(
            "{{ field.name }}", data["{{ field.name }}"]
        )
    {% endfor %}
    return data


def _normalize_model_datetimes(obj: Any) -> None:
    """Normalize model defaults and hook-assigned datetime values as well."""
    {% for field in datetime_fields %}
    value = getattr(obj, "{{ field.name }}", None)
    if isinstance(value, datetime):
        # PostgreSQL returns TIMESTAMP WITHOUT TIME ZONE as a naive datetime.
        # It is already in the column's storage representation; interpreting it
        # as a fresh local form value would shift it and mark an untouched field
        # dirty during unrelated updates.
        if value.tzinfo is not None or _datetime_column_uses_timezone("{{ field.name }}"):
            setattr(
                obj,
                "{{ field.name }}",
                _normalize_datetime_for_storage("{{ field.name }}", value),
            )
    {% endfor %}

{% endif %}
{% if scalar_json_arrays %}
def _serialize_json_scalar_arrays(data: dict) -> dict:
    """Convert date/time objects to JSON-safe ISO strings before persistence."""
    {% for field in scalar_json_arrays %}
    if isinstance(data.get("{{ field.name }}"), list):
        data["{{ field.name }}"] = [
            {% if field.json_item_kind == "datetime" %}
            to_utc(value).isoformat() if isinstance(value, datetime) else value
            {% else %}
            value.isoformat() if hasattr(value, "isoformat") else value
            {% endif %}
            for value in data["{{ field.name }}"]
        ]
    {% endfor %}
    return data

{% endif %}

{% for rfk in model.reverse_foreign_keys %}
{% if rfk.editor == 'inline' %}
async def _sync_inline_{{ rfk.name }}(
    session: AsyncSession,
    parent_id: {{ model.id_type }},
    items: list,
) -> None:
    statement = select({{ rfk.source_model }}).where(
        {{ rfk.source_model }}.{{ rfk.source_fk_field }} == parent_id
    )
    result = await session.exec(statement)
    existing = {item.id: item for item in result.all()}
    kept_ids = set()

    for item in items:
        data = item.model_dump(exclude_unset=True) if hasattr(item, "model_dump") else dict(item)
        item_id = data.pop("id", None)
        data.pop("{{ rfk.source_fk_field }}", None)
        if item_id is None:
            missing = [
                name for name in [
                    {% for field in rfk.inline_fields %}
                    {% if field.required and not field.create_optional %}"{{ field.name }}",{% endif %}
                    {% endfor %}
                ]
                if data.get(name) is None
            ]
            if missing:
                raise ValueError("Missing required {{ rfk.source_model }} fields: " + ", ".join(missing))
            child = {{ rfk.source_model }}(**data)
            setattr(child, "{{ rfk.source_fk_field }}", parent_id)
            session.add(child)
            await session.flush()
            kept_ids.add(child.id)
        else:
            child = existing.get(item_id)
            if child is None:
                raise ValueError("{{ rfk.source_model }} is not related to this {{ model.name }}")
            for key, value in data.items():
                setattr(child, key, value)
            session.add(child)
            kept_ids.add(item_id)

    for item_id, child in existing.items():
        if item_id in kept_ids:
            continue
        {% if rfk.on_remove == 'nullify' %}
        setattr(child, "{{ rfk.source_fk_field }}", None)
        session.add(child)
        {% else %}
        await session.delete(child)
        {% endif %}


{% endif %}
{% endfor %}
{% for m2m in model.m2m_fields %}
{% if m2m.editor == 'inline' %}
async def _sync_inline_{{ m2m.write_name }}(
    session: AsyncSession,
    parent_id: {{ model.id_type }},
    items: list,
) -> None:
    statement = select({{ m2m.link_model }}).where(
        {{ m2m.link_model }}.{{ m2m.source_fk_field }} == parent_id
    )
    result = await session.exec(statement)
    existing_links = list(result.all())
    links_by_target = {
        getattr(link, "{{ m2m.target_fk_field }}"): link for link in existing_links
    }
    kept_ids = set()

    for index, item in enumerate(items, start=1):
        data = item.model_dump(exclude_unset=True) if hasattr(item, "model_dump") else dict(item)
        target_id = data.pop("id", None)
        if target_id is None:
            missing = [
                name for name in [
                    {% for field in m2m.inline_fields %}
                    {% if field.required and not field.create_optional %}"{{ field.name }}",{% endif %}
                    {% endfor %}
                ]
                if data.get(name) is None
            ]
            if missing:
                raise ValueError("Missing required {{ m2m.target_model }} fields: " + ", ".join(missing))
            target = {{ m2m.target_model }}(**data)
            session.add(target)
            await session.flush()
            target_id = target.id
        else:
            link = links_by_target.get(target_id)
            if link is None and not {{ 'True' if m2m.allow_existing else 'False' }}:
                raise ValueError("{{ m2m.target_model }} is not related to this {{ model.name }}")
            target = await session.get({{ m2m.target_model }}, target_id)
            if target is None:
                raise ValueError("{{ m2m.target_model }} not found")
            for key, value in data.items():
                setattr(target, key, value)
            session.add(target)

        link = links_by_target.get(target_id)
        if link is None:
            link = {{ m2m.link_model }}(
                {{ m2m.source_fk_field }}=parent_id,
                {{ m2m.target_fk_field }}=target_id,
            )
            session.add(link)
        {% if m2m.order_field %}
        setattr(link, "{{ m2m.order_field }}", index)
        {% endif %}
        kept_ids.add(target_id)

    for target_id, link in links_by_target.items():
        if target_id not in kept_ids:
            await session.delete(link)


{% endif %}
{% endfor %}
{% if model.is_timescaledb %}

# ── TimescaleDB CRUD ──────────────────────────────────────────────────────────

@transactional_write
async def create(session: AsyncSession, obj_in: {{ model.name }}Create, *, auto_commit: bool = True) -> {{ model.name }}:
    """Insert a timeseries record into hypertable and upsert latest table."""
    {% if scalar_json_arrays %}
    obj_dict = _serialize_json_scalar_arrays(obj_in.dict(exclude_none=True))
    {% else %}
    obj_dict = obj_in.dict(exclude_none=True)
    {% endif %}
    {% if datetime_fields %}
    obj_dict = _normalize_datetimes(obj_dict)
    {% endif %}
    {% if model.timescaledb_model_table and model.timescaledb_metric_field and model.timescaledb_model_class and model.timescaledb_entity_model %}
    # Validate metric_key against {{ model.timescaledb_model_class }}.properties
    entity_id_val = obj_dict.get("{{ model.timescaledb_entity_field }}")
    if entity_id_val is not None:
        # Look up entity FK to find which {{ model.timescaledb_model_class }} it uses
        entity = await session.get({{ model.timescaledb_entity_model }}, entity_id_val)
        model_table_id = getattr(entity, "{{ model.timescaledb_model_table }}_id", None) if entity else None
        if model_table_id is not None:
            from app.models.{{ model.timescaledb_model_table }} import {{ model.timescaledb_model_class }}
            dm = await session.get({{ model.timescaledb_model_class }}, model_table_id)
            if dm and dm.properties:
                valid_keys = set(dm.properties.keys())
                metric_val = obj_dict.get("{{ model.timescaledb_metric_field }}")
                if metric_val is not None and metric_val not in valid_keys:
                    raise ValueError(
                        "Unknown metric '" + str(metric_val) + "'. "
                        "Allowed: " + ", ".join(sorted(valid_keys))
                    )
    {% endif %}
    db_obj = {{ model.name }}(**obj_dict)
    hook_context = ModelHookContext(
        operation="create",
        input_data=dict(obj_dict),
        changed_fields=frozenset(obj_dict),
    )
    await invoke_model_hook(
        db_obj,
        "on_before_create",
        session=session,
        context=hook_context,
    )
    {% if datetime_fields %}
    _normalize_model_datetimes(db_obj)
    {% endif %}
    session.add(db_obj)
    await session.flush()
    await session.refresh(db_obj)

    {% if not is_postgresql %}
    # Upsert latest table (PostgreSQL uses a DB trigger for this)
    from sqlalchemy import text
    entity_val = getattr(db_obj, "{{ model.timescaledb_entity_field }}")
    time_val = getattr(db_obj, "{{ model.timescaledb_time_column }}")
    json_val = db_obj.model_dump_json()
    {% if model.timescaledb_metric_field %}
    metric_val = getattr(db_obj, "{{ model.timescaledb_metric_field }}")
    await session.execute(
        text("""
            INSERT INTO {{ model.timescaledb_latest_table_name }}
            ({{ model.timescaledb_entity_field }}, {{ model.timescaledb_metric_field }}, value, {{ model.timescaledb_time_column }})
            VALUES (:entity, :metric, :value::jsonb, :time)
            ON CONFLICT ({{ model.timescaledb_entity_field }}, {{ model.timescaledb_metric_field }})
            DO UPDATE SET value = :value::jsonb, {{ model.timescaledb_time_column }} = :time
            WHERE :time >= {{ model.timescaledb_latest_table_name }}.{{ model.timescaledb_time_column }}
        """),
        {"entity": entity_val, "metric": metric_val, "value": json_val, "time": time_val},
    )
    {% else %}
    await session.execute(
        text("""
            INSERT INTO {{ model.timescaledb_latest_table_name }}
            ({{ model.timescaledb_entity_field }}, value, {{ model.timescaledb_time_column }})
            VALUES (:entity, :value::jsonb, :time)
            ON CONFLICT ({{ model.timescaledb_entity_field }})
            DO UPDATE SET value = :value::jsonb, {{ model.timescaledb_time_column }} = :time
            WHERE :time >= {{ model.timescaledb_latest_table_name }}.{{ model.timescaledb_time_column }}
        """),
        {"entity": entity_val, "value": json_val, "time": time_val},
    )
    {% endif %}
    {% endif %}

    await invoke_model_hook(
        db_obj,
        "on_after_create",
        session=session,
        context=hook_context,
    )
    await session.flush()
    return db_obj


async def get_realtime_by_entity(
    session: AsyncSession,
    entity_id: str,
) -> list[dict]:
    """Get latest values for each metric of a given entity from the latest table."""
    from sqlalchemy import text
    {% if model.timescaledb_entity_sql_type == "INTEGER" %}
    entity_id = int(entity_id)  # entity_id comes as str from query param; convert for DB
    {% endif %}
    query = text("""
        SELECT *
        FROM {{ model.timescaledb_latest_table_name }}
        WHERE {{ model.timescaledb_entity_field }} = :entity
        ORDER BY {{ model.timescaledb_time_column }} DESC
    """)
    result = await session.execute(query, {"entity": entity_id})
    rows = result.fetchall()
    # Return as list of dicts
    columns = result.keys()
    return [dict(zip(columns, row)) for row in rows]


async def get_history_by_entity(
    session: AsyncSession,
    entity_id: str,
    metric: str | None = None,
    start_time: datetime | None = None,
    end_time: datetime | None = None,
    limit: int = 1000,
) -> list[dict]:
    """Get historical timeseries data for an entity from hypertable."""
    from sqlalchemy import text
    {% if model.timescaledb_entity_sql_type == "INTEGER" %}
    entity_id = int(entity_id)  # entity_id comes as str from query param; convert for DB
    {% endif %}

    conditions = ["{{ model.timescaledb_entity_field }} = :entity"]
    params: dict = {"entity": entity_id}

    {% if model.timescaledb_metric_field %}
    if metric is not None:
        conditions.append("{{ model.timescaledb_metric_field }} = :metric")
        params["metric"] = metric
    {% endif %}
    if start_time is not None:
        conditions.append("{{ model.timescaledb_time_column }} >= :start")
        params["start"] = start_time
    if end_time is not None:
        conditions.append("{{ model.timescaledb_time_column }} <= :end")
        params["end"] = end_time

    where_clause = " AND ".join(conditions)
    query = text(f"""
        SELECT *
        FROM {{ model.table_name }}
        WHERE {where_clause}
        ORDER BY {{ model.timescaledb_time_column }} DESC
        LIMIT NULLIF(:limit, -1)
    """)
    params["limit"] = limit
    result = await session.execute(query, params)
    rows = result.fetchall()
    columns = result.keys()
    return [dict(zip(columns, row)) for row in rows]

{% else %}

async def get(session: AsyncSession, id: {{ model.id_type }}) -> Optional[{{ model.name }}]:
    return await session.get({{ model.name }}, id)


{% if model.fields | selectattr('is_unique') | list %}
async def check_unique_field(
    session: AsyncSession,
    field_name: str,
    value: Any,
    exclude_id: Optional[{{ model.id_type }}] = None,
) -> bool:
    """
    Check if a field value is unique.
    Returns True if unique (no conflict), False if duplicate exists.
    """
    statement = select({{ model.name }}).where(getattr({{ model.name }}, field_name) == value)
    if exclude_id is not None:
        statement = statement.where({{ model.name }}.id != exclude_id)
    result = await session.exec(statement)
    return result.first() is None
{% endif %}

{% if model.importable and model.import_key %}
async def get_by_{{ model.import_key }}(
    session: AsyncSession,
    value: Any,
) -> Optional[{{ model.name }}]:
    """
    Get {{ model.name }} by {{ model.import_key }} value.
    Used for import upsert logic.
    """
    statement = select({{ model.name }}).where(getattr({{ model.name }}, "{{ model.import_key }}") == value)
    result = await session.exec(statement)
    return result.first()
{% endif %}


def _apply_filters(
    statement,
    {% for field in model.fields %}
    {% if field.is_search_field %}
    {% if field.ui_type == 'bool' %}
    {{ field.name }}: Optional[bool] = None,
    {% elif field.ui_type == 'datetime' %}
    {{ field.name }}_from: Optional[datetime] = None,
    {{ field.name }}_to: Optional[datetime] = None,
    {% elif field.ui_type == 'time' %}
    {{ field.name }}: Optional[time] = None,
    {% elif field.is_enum and field.is_multi_select %}
    {{ field.name }}: Optional[List[str]] = None,
    {% elif field.is_enum %}
    {{ field.name }}: Optional[str] = None,
    {% elif field.fk_info %}
    {{ field.name }}: Optional[int] = None,
    {% elif field.ui_type == 'str' %}
    {{ field.name }}: Optional[str] = None,
    {% endif %}
    {% endif %}
    {% endfor %}
    {% set search_field_names = model.fields | selectattr('is_search_field') | map(attribute='name') | list %}
    {% for fk in model.foreign_keys %}
    {% if fk.name != model.owner_field and fk.name not in search_field_names %}
    {{ fk.name }}: Optional[int] = None,
    {% endif %}
    {% endfor %}
    {% if model.owner_field %}
    owner_id: Optional[int] = None,
    {% endif %}
):
    """Apply search and filter conditions to statement. Shared by get_multi and count."""
    # Apply per-field filters (configured via site_props.is_search_field)
    {% for field in model.fields %}
    {% if field.is_search_field %}
    {% if field.ui_type == 'bool' %}
    if {{ field.name }} is not None:
        statement = statement.where({{ model.name }}.{{ field.name }} == {{ field.name }})
    {% elif field.ui_type == 'datetime' %}
    if {{ field.name }}_from is not None:
        if isinstance({{ field.name }}_from, datetime):
            {{ field.name }}_from = to_utc({{ field.name }}_from)
        statement = statement.where({{ model.name }}.{{ field.name }} >= {{ field.name }}_from)
    if {{ field.name }}_to is not None:
        if isinstance({{ field.name }}_to, datetime):
            {{ field.name }}_to = to_utc({{ field.name }}_to)
        statement = statement.where({{ model.name }}.{{ field.name }} <= {{ field.name }}_to)
    {% elif field.ui_type == 'time' %}
    if {{ field.name }} is not None:
        statement = statement.where({{ model.name }}.{{ field.name }} == {{ field.name }})
    {% elif field.is_enum and field.is_multi_select %}
    if {{ field.name }}:
        for _v in {{ field.name }}:
            _sub = (
                select(1)
                .select_from(func.json_each({{ model.name }}.{{ field.name }}).table_valued("value"))
                .where(func.json_each.table_valued("value").valued_column("value") == _v)
                .correlate({{ model.name }})
                .exists()
            )
            statement = statement.where(_sub)
    {% elif field.is_enum %}
    if {{ field.name }}:
        statement = statement.where({{ model.name }}.{{ field.name }} == {{ field.name }})
    {% elif field.fk_info %}
    if {{ field.name }} is not None:
        statement = statement.where({{ model.name }}.{{ field.name }} == {{ field.name }})
    {% elif field.ui_type == 'str' %}
    if {{ field.name }}:
        statement = statement.where(col({{ model.name }}.{{ field.name }}).ilike("%" + {{ field.name }} + "%"))
    {% endif %}
    {% endif %}
    {% endfor %}

    # Apply foreign key filters (0 = sentinel for IS NULL)
    {% for fk in model.foreign_keys %}
    {% if fk.name != model.owner_field and fk.name not in search_field_names %}
    if {{ fk.name }} is not None:
        if {{ fk.name }} == 0:
            statement = statement.where({{ model.name }}.{{ fk.name }}.is_(None))
        else:
            statement = statement.where({{ model.name }}.{{ fk.name }} == {{ fk.name }})
    {% endif %}
    {% endfor %}

    {% if model.owner_field %}
    # Apply owner filter (user-scoped access)
    if owner_id is not None:
        statement = statement.where({{ model.name }}.{{ model.owner_field }} == owner_id)
    {% endif %}

    return statement


async def get_multi(
    session: AsyncSession,
    *,
    page: int = 1,
    size: int = 20,
    {% for field in model.fields %}
    {% if field.is_search_field %}
    {% if field.ui_type == 'bool' %}
    {{ field.name }}: Optional[bool] = None,
    {% elif field.ui_type == 'datetime' %}
    {{ field.name }}_from: Optional[datetime] = None,
    {{ field.name }}_to: Optional[datetime] = None,
    {% elif field.ui_type == 'time' %}
    {{ field.name }}: Optional[time] = None,
    {% elif field.is_enum and field.is_multi_select %}
    {{ field.name }}: Optional[List[str]] = None,
    {% elif field.is_enum %}
    {{ field.name }}: Optional[str] = None,
    {% elif field.fk_info %}
    {{ field.name }}: Optional[int] = None,
    {% elif field.ui_type == 'str' %}
    {{ field.name }}: Optional[str] = None,
    {% endif %}
    {% endif %}
    {% endfor %}
    {% for fk in model.foreign_keys %}
    {% if fk.name != model.owner_field and fk.name not in search_field_names %}
    {{ fk.name }}: Optional[int] = None,
    {% endif %}
    {% endfor %}
    {% if model.owner_field %}
    owner_id: Optional[int] = None,
    {% endif %}
) -> List[{{ model.name }}]:
    """
    Get list of objects with optional search and filter support.
    When size=-1, returns all matching records without pagination.
    Returns only the list of objects; use count() for total.
    """
    statement = select({{ model.name }})
    statement = _apply_filters(
        statement,
        {% for field in model.fields %}
        {% if field.is_search_field %}
        {% if field.ui_type == 'bool' %}
        {{ field.name }}={{ field.name }},
        {% elif field.ui_type == 'datetime' %}
        {{ field.name }}_from={{ field.name }}_from,
        {{ field.name }}_to={{ field.name }}_to,
        {% elif field.is_enum %}
        {{ field.name }}={{ field.name }},
        {% elif field.fk_info %}
        {{ field.name }}={{ field.name }},
        {% elif field.ui_type == 'str' %}
        {{ field.name }}={{ field.name }},
        {% endif %}
        {% endif %}
        {% endfor %}
        {% for fk in model.foreign_keys %}
        {% if fk.name != model.owner_field and fk.name not in search_field_names %}
        {{ fk.name }}={{ fk.name }},
        {% endif %}
        {% endfor %}
        {% if model.owner_field %}
        owner_id=owner_id,
        {% endif %}
    )

    {% if model.has_created_at %}
    statement = statement.order_by(col({{ model.name }}.created_at).desc())
    {% endif %}

    # Apply pagination (skip if size == -1, meaning get all)
    if size != -1:
        skip = (page - 1) * size
        statement = statement.offset(skip).limit(size)

    result = await session.exec(statement)
    return list(result.all())


async def get_multi_with_fk_labels(
    session: AsyncSession,
    *,
    page: int = 1,
    size: int = 20,
    {% for field in model.fields %}
    {% if field.is_search_field %}
    {% if field.ui_type == 'bool' %}
    {{ field.name }}: Optional[bool] = None,
    {% elif field.ui_type == 'datetime' %}
    {{ field.name }}_from: Optional[datetime] = None,
    {{ field.name }}_to: Optional[datetime] = None,
    {% elif field.ui_type == 'time' %}
    {{ field.name }}: Optional[time] = None,
    {% elif field.is_enum and field.is_multi_select %}
    {{ field.name }}: Optional[List[str]] = None,
    {% elif field.is_enum %}
    {{ field.name }}: Optional[str] = None,
    {% elif field.fk_info %}
    {{ field.name }}: Optional[int] = None,
    {% elif field.ui_type == 'str' %}
    {{ field.name }}: Optional[str] = None,
    {% endif %}
    {% endif %}
    {% endfor %}
    {% for fk in model.foreign_keys %}
    {% if fk.name != model.owner_field and fk.name not in search_field_names %}
    {{ fk.name }}: Optional[int] = None,
    {% endif %}
    {% endfor %}
    {% if model.owner_field %}
    owner_id: Optional[int] = None,
    {% endif %}
) -> List[Dict[str, Any]]:
    """
    Get list of objects with FK label fields enriched.
    Returns list of dicts with original fields plus {fk_name}_label for each FK.
    """
    # Get base items
    items = await get_multi(
        session,
        page=page,
        size=size,
        {% for field in model.fields %}
        {% if field.is_search_field %}
        {% if field.ui_type == 'bool' %}
        {{ field.name }}={{ field.name }},
        {% elif field.ui_type == 'datetime' %}
        {{ field.name }}_from={{ field.name }}_from,
        {{ field.name }}_to={{ field.name }}_to,
        {% elif field.is_enum %}
        {{ field.name }}={{ field.name }},
        {% elif field.fk_info %}
        {{ field.name }}={{ field.name }},
        {% elif field.ui_type == 'str' %}
        {{ field.name }}={{ field.name }},
        {% endif %}
        {% endif %}
        {% endfor %}
        {% for fk in model.foreign_keys %}
        {% if fk.name != model.owner_field and fk.name not in search_field_names %}
        {{ fk.name }}={{ fk.name }},
        {% endif %}
        {% endfor %}
        {% if model.owner_field %}
        owner_id=owner_id,
        {% endif %}
    )

    if not items:
        return []
    
    {% if model.foreign_keys %}
    # Collect unique FK IDs for batch fetch
    {% for fk in model.foreign_keys %}
    {{ fk.name }}_ids = list(set(getattr(item, "{{ fk.name }}") for item in items if getattr(item, "{{ fk.name }}") is not None))
    {% endfor %}
    
    # Batch fetch FK targets
    {% for fk in model.foreign_keys %}
    {{ fk.name }}_map = {}
    if {{ fk.name }}_ids:
        from app.models.{{ fk.target_source_module }} import {{ fk.target_model }}
        stmt = select({{ fk.target_model }}).where({{ fk.target_model }}.id.in_({{ fk.name }}_ids))
        result = await session.exec(stmt)
        for obj in result.all():
            {{ fk.name }}_map[obj.id] = getattr(obj, "{{ fk.label_field }}", None) or str(obj.id)
    {% endfor %}
    
    # Enrich items with labels
    enriched = []
    for item in items:
        item_dict = item.model_dump()
        {% for fk in model.foreign_keys %}
        fk_id = getattr(item, "{{ fk.name }}", None)
        item_dict["{{ fk.name }}_label"] = {{ fk.name }}_map.get(fk_id) if fk_id else None
        {% endfor %}
        enriched.append(item_dict)
    
    return enriched
    {% else %}
    # No FKs, just convert to dicts
    return [item.model_dump() for item in items]
    {% endif %}


async def count(
    session: AsyncSession,
    {% for field in model.fields %}
    {% if field.is_search_field %}
    {% if field.ui_type == 'bool' %}
    {{ field.name }}: Optional[bool] = None,
    {% elif field.ui_type == 'datetime' %}
    {{ field.name }}_from: Optional[datetime] = None,
    {{ field.name }}_to: Optional[datetime] = None,
    {% elif field.ui_type == 'time' %}
    {{ field.name }}: Optional[time] = None,
    {% elif field.is_enum and field.is_multi_select %}
    {{ field.name }}: Optional[List[str]] = None,
    {% elif field.is_enum %}
    {{ field.name }}: Optional[str] = None,
    {% elif field.fk_info %}
    {{ field.name }}: Optional[int] = None,
    {% elif field.ui_type == 'str' %}
    {{ field.name }}: Optional[str] = None,
    {% endif %}
    {% endif %}
    {% endfor %}
    {% for fk in model.foreign_keys %}
    {% if fk.name != model.owner_field and fk.name not in search_field_names %}
    {{ fk.name }}: Optional[int] = None,
    {% endif %}
    {% endfor %}
    {% if model.owner_field %}
    owner_id: Optional[int] = None,
    {% endif %}
) -> int:
    """
    Count total matching records with same filter conditions as get_multi.
    """
    statement = select(func.count()).select_from({{ model.name }})
    statement = _apply_filters(
        statement,
        {% for field in model.fields %}
        {% if field.is_search_field %}
        {% if field.ui_type == 'bool' %}
        {{ field.name }}={{ field.name }},
        {% elif field.ui_type == 'datetime' %}
        {{ field.name }}_from={{ field.name }}_from,
        {{ field.name }}_to={{ field.name }}_to,
        {% elif field.is_enum %}
        {{ field.name }}={{ field.name }},
        {% elif field.fk_info %}
        {{ field.name }}={{ field.name }},
        {% elif field.ui_type == 'str' %}
        {{ field.name }}={{ field.name }},
        {% endif %}
        {% endif %}
        {% endfor %}
        {% for fk in model.foreign_keys %}
        {% if fk.name != model.owner_field and fk.name not in search_field_names %}
        {{ fk.name }}={{ fk.name }},
        {% endif %}
        {% endfor %}
        {% if model.owner_field %}
        owner_id=owner_id,
        {% endif %}
    )

    result = await session.exec(statement)
    return result.one()

@transactional_write
async def create(session: AsyncSession, obj_in: {{ model.name }}Create, *, auto_commit: bool = True) -> {{ model.name }}:
    # Note: validation should happen before, here we assume obj_in is valid pydantic model
    
    # Separate relationship payloads from concrete {{ model.name }} columns.
    {% if model.m2m_fields %}
    m2m_data = {}
    {% for m2m in model.m2m_fields %}
    if hasattr(obj_in, "{{ m2m.write_name }}"):
        m2m_data["{{ m2m.write_name }}"] = getattr(obj_in, "{{ m2m.write_name }}")
        delattr(obj_in, "{{ m2m.write_name }}")
    {% endfor %}
    {% endif %}
    inline_reverse_data = {}
    {% for rfk in model.reverse_foreign_keys %}
    {% if rfk.editor == 'inline' %}
    if hasattr(obj_in, "{{ rfk.write_name }}"):
        inline_reverse_data["{{ rfk.write_name }}"] = getattr(obj_in, "{{ rfk.write_name }}")
        delattr(obj_in, "{{ rfk.write_name }}")
    {% endif %}
    {% endfor %}

    # Convert to dict to handle nested Pydantic models in JSON fields
    {% if scalar_json_arrays %}
    obj_dict = _serialize_json_scalar_arrays(obj_in.dict(exclude_none=True))
    {% else %}
    obj_dict = obj_in.dict(exclude_none=True)
    {% endif %}
    {% if datetime_fields %}
    obj_dict = _normalize_datetimes(obj_dict)
    {% endif %}

    {% if model.property_config_meta %}
    # Initialize properties_config from blueprint model if not explicitly provided
    if not obj_dict.get("{{ model.property_config_meta.field_name }}"):
        blueprint_id = obj_dict.get("{{ model.property_config_meta.blueprint_fk }}")
        if blueprint_id is not None:
            blueprint = await session.get({{ model.property_config_meta.blueprint_model }}, blueprint_id)
            if blueprint and getattr(blueprint, "{{ model.property_config_meta.blueprint_properties_field }}", None):
                props = getattr(blueprint, "{{ model.property_config_meta.blueprint_properties_field }}")
                config_entries: dict[str, Any] = {}
                for key, p in props.items():
                    cfg = {{ model.property_config_meta.config_class }}(
                        {% for field_name, field_type in model.property_config_meta.config_fields.items() %}
                        {% set ft = field_type.lower() %}
                        {% if '= true' in ft %}
                        {{ field_name }}=True,
                        {% elif '= false' in ft %}
                        {{ field_name }}=False,
                        {% elif '= ' in ft %}
                        {{ field_name }}={{ field_type.split('=', 1)[1].strip() }},
                        {% elif 'optional' in ft %}
                        {{ field_name }}=None,
                        {% else %}
                        {% endif %}
                        {% endfor %}
                    )
                    config_entries[key] = cfg.model_dump()
                obj_dict["{{ model.property_config_meta.field_name }}"] = config_entries
    {% endif %}

    db_obj = {{ model.name }}(**obj_dict)
    {% if model.fields | selectattr('name', 'equalto', 'updated_at') | list %}
    db_obj.updated_at = datetime.now(timezone.utc)
    {% endif %}
    hook_context = ModelHookContext(
        operation="create",
        input_data=dict(obj_dict),
        changed_fields=frozenset(obj_dict),
    )
    await invoke_model_hook(
        db_obj,
        "on_before_create",
        session=session,
        context=hook_context,
    )
    {% if datetime_fields %}
    _normalize_model_datetimes(db_obj)
    {% endif %}
    session.add(db_obj)
    await session.flush()
    await session.refresh(db_obj)
    
    {% if model.m2m_fields %}
    # Insert M2M relations
    {% for m2m in model.m2m_fields %}
    {% if m2m.editor == 'inline' %}
    if m2m_data.get("{{ m2m.write_name }}") is not None:
        await _sync_inline_{{ m2m.write_name }}(session, db_obj.id, m2m_data["{{ m2m.write_name }}"])
    {% else %}
    if m2m_data.get("{{ m2m.write_name }}") is not None:
        for idx, target_id in enumerate(m2m_data["{{ m2m.write_name }}"], start=1):
            link = {{ m2m.link_model }}(
                {{ m2m.source_fk_field }}=db_obj.id,
                {{ m2m.target_fk_field }}=target_id
                {% if m2m.order_field %}
                , {{ m2m.order_field }}=idx
                {% endif %}
            )
            session.add(link)
    {% endif %}
    {% endfor %}
    await session.flush()
    await session.refresh(db_obj)
    {% endif %}

    {% for rfk in model.reverse_foreign_keys %}
    {% if rfk.editor == 'inline' %}
    if inline_reverse_data.get("{{ rfk.write_name }}") is not None:
        await _sync_inline_{{ rfk.name }}(
            session, db_obj.id, inline_reverse_data["{{ rfk.write_name }}"]
        )
    {% endif %}
    {% endfor %}

    await invoke_model_hook(
        db_obj,
        "on_after_create",
        session=session,
        context=hook_context,
    )
    await session.flush()
    return db_obj

@transactional_write
async def update(session: AsyncSession, db_obj: {{ model.name }}, obj_in: {{ model.name }}Update, *, auto_commit: bool = True) -> {{ model.name }}:
    {% if scalar_json_arrays %}
    obj_data = _serialize_json_scalar_arrays(obj_in.dict(exclude_unset=True))
    {% else %}
    obj_data = obj_in.dict(exclude_unset=True)
    {% endif %}
    {% if datetime_fields %}
    obj_data = _normalize_datetimes(obj_data)
    {% endif %}
    inline_reverse_data = {}
    {% for rfk in model.reverse_foreign_keys %}
    {% if rfk.editor == 'inline' %}
    if "{{ rfk.write_name }}" in obj_data:
        inline_reverse_data["{{ rfk.write_name }}"] = obj_data.pop("{{ rfk.write_name }}")
    {% endif %}
    {% endfor %}
    
    old = snapshot_model(db_obj)
    hook_context = ModelHookContext(
        operation="update",
        input_data=dict(obj_data),
        changed_fields=frozenset(obj_data),
    )
    await invoke_model_hook(
        db_obj,
        "on_before_update",
        session=session,
        old=old,
        changes=obj_data,
        context=hook_context,
    )

    {% if model.m2m_fields %}
    # Handle M2M updates
    {% for m2m in model.m2m_fields %}
    if "{{ m2m.write_name }}" in obj_data:
        new_ids = obj_data.pop("{{ m2m.write_name }}")
        if new_ids is not None:
            {% if m2m.editor == 'inline' %}
            await _sync_inline_{{ m2m.write_name }}(session, db_obj.id, new_ids)
            {% else %}
            # 1. Clear existing relations
            # This is a naive implementation (delete all and re-insert). 
            # Better would be to calculate diff.
            statement = select({{ m2m.link_model }}).where({{ m2m.link_model }}.{{ m2m.source_fk_field }} == db_obj.id)
            result = await session.exec(statement)
            existing_links = result.all()
            for link in existing_links:
                await session.delete(link)
            
            # 2. Insert new relations
            for idx, target_id in enumerate(new_ids, start=1):
                link = {{ m2m.link_model }}(
                    {{ m2m.source_fk_field }}=db_obj.id,
                    {{ m2m.target_fk_field }}=target_id
                    {% if m2m.order_field %}
                    , {{ m2m.order_field }}=idx
                    {% endif %}
                )
                session.add(link)
            {% endif %}
    {% endfor %}
    {% endif %}

    {% for rfk in model.reverse_foreign_keys %}
    {% if rfk.editor == 'inline' %}
    if inline_reverse_data.get("{{ rfk.write_name }}") is not None:
        await _sync_inline_{{ rfk.name }}(
            session, db_obj.id, inline_reverse_data["{{ rfk.write_name }}"]
        )
    {% endif %}
    {% endfor %}
    
    {% if model.property_config_meta %}
    # Capture old blueprint FK before setattr overwrites it
    _old_blueprint_id = getattr(db_obj, "{{ model.property_config_meta.blueprint_fk }}", None)
    {% endif %}

    for key, value in obj_data.items():
        {% if model.property_config_meta %}
        if key == "{{ model.property_config_meta.field_name }}" and value is not None:
            # Validate add/delete permissions (dict keys)
            field_perms = "{{ model.property_config_meta.field_permissions }}"
            old_configs = getattr(db_obj, "{{ model.property_config_meta.field_name }}", None) or {}
            old_keys = set(old_configs.keys())
            new_keys = set(value.keys())
            added = new_keys - old_keys
            if added and "a" not in field_perms:
                raise ValueError("Adding items is not allowed: " + ", ".join(sorted(added)))
            removed = old_keys - new_keys
            if removed and "l" not in field_perms:
                raise ValueError("Deleting items is not allowed: " + ", ".join(sorted(removed)))
            # Convert Pydantic objects to dicts for JSON storage
            value = {k: (v if isinstance(v, dict) else v.model_dump()) for k, v in value.items()}
        {% endif %}
        setattr(db_obj, key, value)

    {% if model.property_config_meta %}
    # Re-initialize properties_config if blueprint FK changed
    new_bp = obj_data.get("{{ model.property_config_meta.blueprint_fk }}")
    if new_bp is not None and new_bp != _old_blueprint_id:
        blueprint = await session.get({{ model.property_config_meta.blueprint_model }}, new_bp)
        if blueprint and getattr(blueprint, "{{ model.property_config_meta.blueprint_properties_field }}", None):
            props = getattr(blueprint, "{{ model.property_config_meta.blueprint_properties_field }}")
            config_entries: dict[str, Any] = {}
            for key, p in props.items():
                cfg = {{ model.property_config_meta.config_class }}(
                    {% for field_name, field_type in model.property_config_meta.config_fields.items() %}
                    {% set ft = field_type.lower() %}
                    {% if '= true' in ft %}
                    {{ field_name }}=True,
                    {% elif '= false' in ft %}
                    {{ field_name }}=False,
                    {% elif '= ' in ft %}
                    {{ field_name }}={{ field_type.split('=', 1)[1].strip() }},
                    {% elif 'optional' in ft %}
                    {{ field_name }}=None,
                    {% else %}
                    {% endif %}
                    {% endfor %}
                )
                config_entries[key] = cfg.model_dump()
            setattr(db_obj, "{{ model.property_config_meta.field_name }}", config_entries)
    {% endif %}

    {% if model.fields | selectattr('name', 'equalto', 'updated_at') | list %}
    db_obj.updated_at = datetime.now(timezone.utc)
    {% endif %}
    {% if datetime_fields %}
    _normalize_model_datetimes(db_obj)
    {% endif %}
    session.add(db_obj)
    await session.flush()
    await session.refresh(db_obj)
    await invoke_model_hook(
        db_obj,
        "on_after_update",
        session=session,
        old=old,
        changes=obj_data,
        context=hook_context,
    )
    await session.flush()
    return db_obj

@transactional_write
async def delete(session: AsyncSession, id: {{ model.id_type }}, *, auto_commit: bool = True) -> {{ model.id_type }}:
    db_obj = await session.get({{ model.name }}, id)
    old = snapshot_model(db_obj)
    hook_context = ModelHookContext(
        operation="delete",
        input_data={},
        changed_fields=frozenset(),
    )
    await invoke_model_hook(
        db_obj,
        "on_before_delete",
        session=session,
        old=old,
        context=hook_context,
    )
    deleted_id = db_obj.id
    await session.delete(db_obj)
    await session.flush()
    await invoke_model_hook(
        db_obj,
        "on_after_delete",
        session=session,
        old=old,
        context=hook_context,
    )
    await session.flush()
    return deleted_id

{% if model.is_notification_table %}
@transactional_write
async def mark_all_read_by_user(
    session: AsyncSession,
    user_id: int,
    *,
    auto_commit: bool = True,
) -> int:
    """
    Mark all unread notifications as read for a specific user.
    Returns the number of notifications updated.
    """
    from sqlmodel import update

    statement = (
        update({{ model.name }})
        .where({{ model.name }}.user_id == user_id)
        .where({{ model.name }}.is_read == False)
        .values(is_read=True)
    )
    result = await session.exec(statement)
    return result.rowcount
{% endif %}

{% if model.m2m_fields %}
# M2M Relationship CRUD functions
{% for m2m in model.m2m_fields %}
async def get_{{ m2m.target_endpoint }}(
    session: AsyncSession,
    {{ model.lower_name }}_id: {{ model.id_type }},
    page: int = 1,
    size: int = 20,
) -> List[Dict[str, Any]]:
    """Get list of {{ m2m.target_model }} objects related to {{ model.name }}, enriched with FK labels."""
    # Get target IDs first
    statement = select({{ m2m.link_model }}.{{ m2m.target_fk_field }}).where(
        {{ m2m.link_model }}.{{ m2m.source_fk_field }} == {{ model.lower_name }}_id
    )
    result = await session.exec(statement)
    target_ids = list(result.all())

    if not target_ids:
        return []

    # Pagination
    skip = (page - 1) * size
    paginated_ids = target_ids[skip:skip + size] if size != -1 else target_ids

    # Get objects by IDs
    statement = select({{ m2m.target_model }}).where(
        col({{ m2m.target_model }}.id).in_(paginated_ids)
    )
    result = await session.exec(statement)
    items = list(result.all())

    {% if m2m.target_fk_fields %}
    # Import FK target models for label resolution (avoid shadowing {{ m2m.target_model }})
    {% for fk_field in m2m.target_fk_fields %}
    {% if fk_field.target_model != m2m.target_model %}
    from app.models.{{ fk_field.target_source_module }} import {{ fk_field.target_model }}
    {% endif %}
    {% endfor %}

    # Enrich items with FK labels
    {% for fk_field in m2m.target_fk_fields %}
    {{ fk_field.name }}_ids = list(set(getattr(item, "{{ fk_field.name }}") for item in items if getattr(item, "{{ fk_field.name }}") is not None))
    {% endfor %}

    {% for fk_field in m2m.target_fk_fields %}
    {{ fk_field.name }}_map = {}
    if {{ fk_field.name }}_ids:
        stmt = select({{ fk_field.target_model }}).where({{ fk_field.target_model }}.id.in_({{ fk_field.name }}_ids))
        result = await session.exec(stmt)
        for obj in result.all():
            {{ fk_field.name }}_map[obj.id] = getattr(obj, "{{ fk_field.label_field }}", None) or str(obj.id)
    {% endfor %}

    enriched = []
    for item in items:
        item_dict = item.model_dump()
        {% for fk_field in m2m.target_fk_fields %}
        fk_id = getattr(item, "{{ fk_field.name }}", None)
        item_dict["{{ fk_field.name }}_label"] = {{ fk_field.name }}_map.get(fk_id) if fk_id else None
        {% endfor %}
        enriched.append(item_dict)

    return enriched
    {% else %}
    return [item.model_dump() for item in items]
    {% endif %}


async def count_{{ m2m.target_endpoint }}(session: AsyncSession, {{ model.lower_name }}_id: {{ model.id_type }}) -> int:
    """Count {{ m2m.target_model }}s related to {{ model.name }}."""
    statement = select(func.count()).select_from({{ m2m.link_model }}).where(
        {{ m2m.link_model }}.{{ m2m.source_fk_field }} == {{ model.lower_name }}_id
    )
    result = await session.exec(statement)
    return result.one()

{% if m2m.order_field %}
async def get_{{ m2m.target_endpoint }}_with_order(
    session: AsyncSession,
    {{ model.lower_name }}_id: {{ model.id_type }},
    page: int = 1,
    size: int = 50,
) -> tuple[List[tuple], int]:
    """Get {{ m2m.target_model }}s with order field. Returns (items, total)."""
    skip = (page - 1) * size
    
    # Count total
    total = await count_{{ m2m.target_endpoint }}(session, {{ model.lower_name }}_id)
    
    if total == 0:
        return [], 0
    
    # Query with join
    from app.models.{{ m2m.target_source_module }} import {{ m2m.target_model }}
    statement = (
        select({{ m2m.target_model }}.id, {{ m2m.target_model }}.{{ m2m.label_field }}, {{ m2m.link_model }}.{{ m2m.order_field }})
        .join({{ m2m.link_model }}, {{ m2m.link_model }}.{{ m2m.target_fk_field }} == {{ m2m.target_model }}.id)
        .where({{ m2m.link_model }}.{{ m2m.source_fk_field }} == {{ model.lower_name }}_id)
        .order_by({{ m2m.link_model }}.{{ m2m.order_field }})
        .offset(skip)
        .limit(size)
    )
    result = await session.exec(statement)
    rows = result.all()
    
    return rows, total
{% endif %}
{% endfor %}
{% endif %}

{% if model.reverse_m2m %}
# Reverse M2M Relationship CRUD functions
{% for rel in model.reverse_m2m %}
from app.models.{{ rel.link_module }} import {{ rel.link_model }}

async def get_{{ rel.name }}(
    session: AsyncSession,
    {{ model.lower_name }}_id: {{ model.id_type }},
    page: int = 1,
    size: int = 20,
) -> List[Dict[str, Any]]:
    """Get list of {{ rel.source_model }} objects related to {{ model.name }} (reverse M2M), enriched with FK labels."""
    # Get source IDs first
    statement = select({{ rel.link_model }}.{{ rel.source_fk_field }}).where(
        {{ rel.link_model }}.{{ rel.target_fk_field }} == {{ model.lower_name }}_id
    )
    result = await session.exec(statement)
    source_ids = list(result.all())

    if not source_ids:
        return []

    # Pagination
    skip = (page - 1) * size
    paginated_ids = source_ids[skip:skip + size] if size != -1 else source_ids

    # Get objects by IDs
    statement = select({{ rel.source_model }}).where(
        col({{ rel.source_model }}.id).in_(paginated_ids)
    )
    result = await session.exec(statement)
    items = list(result.all())

    {% if rel.source_fk_fields %}
    # Import FK target models for label resolution (avoid shadowing {{ rel.source_model }})
    {% for fk_field in rel.source_fk_fields %}
    {% if fk_field.target_model != rel.source_model %}
    from app.models.{{ fk_field.target_source_module }} import {{ fk_field.target_model }}
    {% endif %}
    {% endfor %}

    # Enrich items with FK labels
    {% for fk_field in rel.source_fk_fields %}
    {{ fk_field.name }}_ids = list(set(getattr(item, "{{ fk_field.name }}") for item in items if getattr(item, "{{ fk_field.name }}") is not None))
    {% endfor %}

    {% for fk_field in rel.source_fk_fields %}
    {{ fk_field.name }}_map = {}
    if {{ fk_field.name }}_ids:
        stmt = select({{ fk_field.target_model }}).where({{ fk_field.target_model }}.id.in_({{ fk_field.name }}_ids))
        result = await session.exec(stmt)
        for obj in result.all():
            {{ fk_field.name }}_map[obj.id] = getattr(obj, "{{ fk_field.label_field }}", None) or str(obj.id)
    {% endfor %}

    enriched = []
    for item in items:
        item_dict = item.model_dump()
        {% for fk_field in rel.source_fk_fields %}
        fk_id = getattr(item, "{{ fk_field.name }}", None)
        item_dict["{{ fk_field.name }}_label"] = {{ fk_field.name }}_map.get(fk_id) if fk_id else None
        {% endfor %}
        enriched.append(item_dict)

    return enriched
    {% else %}
    return [item.model_dump() for item in items]
    {% endif %}


async def count_{{ rel.name }}(session: AsyncSession, {{ model.lower_name }}_id: {{ model.id_type }}) -> int:
    """Count {{ rel.source_model }}s related to {{ model.name }} (reverse M2M)."""
    statement = select(func.count()).select_from({{ rel.link_model }}).where(
        {{ rel.link_model }}.{{ rel.target_fk_field }} == {{ model.lower_name }}_id
    )
    result = await session.exec(statement)
    return result.one()
{% endfor %}
{% endif %}

{% if model.property_config_meta %}
@transactional_write
async def update_properties_config(
    session: AsyncSession,
    entity_id: {{ model.id_type }},
    configs: list[dict],
    *,
    auto_commit: bool = True,
) -> dict:
    """Update properties_config on entity, with add/delete permission checks."""
    entity = await session.get({{ model.name }}, entity_id)
    if not entity:
        raise ValueError("{{ model.name }} not found")

    field_perms = "{{ model.property_config_meta.field_permissions }}"
    old_keys = set(getattr(entity, "{{ model.property_config_meta.field_name }}", {}).keys())
    new_keys = set(configs.keys())

    # Check add permission
    added = new_keys - old_keys
    if added and "a" not in field_perms:
        raise ValueError(
            "Adding items is not allowed: " + ", ".join(sorted(added))
        )
    # Check delete permission
    removed = old_keys - new_keys
    if removed and "l" not in field_perms:
        raise ValueError(
            "Deleting items is not allowed: " + ", ".join(sorted(removed))
        )

    # Validate config entries with model class
    validated: dict[str, Any] = {}
    for key, c in configs.items():
        if isinstance(c, dict):
            validated[key] = {{ model.property_config_meta.config_class }}(**c).model_dump()
        elif hasattr(c, "model_dump"):
            validated[key] = c.model_dump()
        else:
            validated[key] = c

    setattr(entity, "{{ model.property_config_meta.field_name }}", validated)
    session.add(entity)
    await session.refresh(entity)
    return entity.model_dump()
{% endif %}

{% if model.union_key %}
# Union Key (Composite Key) lookup
async def get_by_{{ model.union_key | join('_') }}(
    session: AsyncSession,
    {% for key_field in model.union_key %}
    {{ key_field }}: {% if key_field.endswith('_id') %}int{% else %}str{% endif %}{% if not loop.last %},
    {% endif %}
    {% endfor %}
) -> Optional[{{ model.name }}]:
    """Get {{ model.name }} by composite key: {{ model.union_key | join(', ') }}."""
    statement = select({{ model.name }}).where(
        {% for key_field in model.union_key %}
        {{ model.name }}.{{ key_field }} == {{ key_field }}{% if not loop.last %},
        {% endif %}
        {% endfor %}
    )
    result = await session.exec(statement)
    return result.first()
{% endif %}
{% endif %}
